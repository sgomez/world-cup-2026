name: Docker Migrate

on:
  workflow_dispatch:

concurrency:
  group: docker-migrate
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}-migrate

jobs:
  build-and-push:
    name: Build & push migrate image
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache-migrate
          key: ${{ runner.os }}-buildx-migrate-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-migrate-

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=sha-,format=short

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.migrations
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=local,src=/tmp/.buildx-cache-migrate
          cache-to: type=local,dest=/tmp/.buildx-cache-migrate-new,mode=max

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache-migrate
          mv /tmp/.buildx-cache-migrate-new /tmp/.buildx-cache-migrate
